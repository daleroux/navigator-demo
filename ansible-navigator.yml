---
ansible-navigator:
  ansible:
    config:
      path: ./ansible.cfg

  execution-environment:
    container-engine: podman
    enabled: true
    image: ee-supported-rhel8:latest
    pull:
      policy: missing

#  mode: stdout

  playbook-artifact:
    enable: false
